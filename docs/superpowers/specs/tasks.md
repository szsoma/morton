Fix these issues:
1. make sure that in the advantages section the images' aspect ratio is 2:3 and
fit: contain
2. Make sure that the hero content (Morton backpacks) text is always fill it's parent container and don't breat the line
3. in the info section the CTA (learn more) should be button with hover
4. the Contact section's CTA (send message) is a button with CTA. In the footer you have to export the white logo "logo" element in figma. It should be 100% container width
